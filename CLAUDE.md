# CLAUDE.md — AI 정서 케어 앱 프론트엔드 설계 프로젝트

## 프로젝트 개요

이 저장소는 **AI 정서 케어 앱(Mio)** 의 프론트엔드 설계 문서를 관리합니다.  
실제 구현 코드가 아닌 **설계 문서(Markdown)** 를 작성·관리하는 것이 이 프로젝트의 목적입니다.

---

## ⚠️ 최우선 원칙: 반드시 먼저 질문하라 ⚠️

> **이 원칙은 이 프로젝트에서 가장 중요한 규칙입니다. 예외 없이 적용됩니다.**

설계 작업을 시작하기 전, **반드시 사용자에게 질문을 먼저** 해야 합니다.

### AI가 절대로 해서는 안 되는 행동
- 질문 없이 스스로 판단해서 설계를 진행하는 것
- "이렇게 설계하면 좋을 것 같습니다"라고 제안하며 먼저 구현하는 것
- 사용자의 의도를 추측해서 문서를 작성하는 것

### AI가 반드시 해야 하는 행동
1. 사용자가 설계 작업을 요청하면, 먼저 **구체적인 질문 목록**을 제시한다
2. 사용자의 답변을 받은 뒤에만 설계를 시작한다
3. 답변이 모호하거나 불충분하면, 추가 질문을 다시 한다

### 질문해야 할 내용의 예시
- 해당 화면/기능의 목적과 핵심 사용자 경험이 무엇인가?
- 어떤 컴포넌트 또는 상태가 필요한가?
- 기존 설계와 어떻게 연결되어야 하는가?
- 어떤 엣지 케이스나 예외 처리가 필요한가?
- 디자인·UX 상의 특이사항이 있는가?

---

## 문서 구조

모든 설계 문서는 `frontend-design/` 디렉토리에 카테고리별로 분리되어 있습니다.

```
files/
├── CLAUDE.md                        ← 이 파일
├── frontend-design/
│   ├── index.md                     ← 마스터 인덱스 (항상 여기서 시작)
│   ├── 01-overview.md
│   ├── 02-tech-stack.md
│   ├── 03-navigation.md
│   ├── 04-pages-onboarding.md
│   ├── 04-pages-home.md
│   ├── 04-pages-checkin.md
│   ├── 04-pages-chat.md
│   ├── 04-pages-report.md
│   ├── 04-pages-mypage.md
│   ├── 04-pages-mind-explore.md
│   ├── 05-components.md
│   ├── 06-folder-structure.md
│   ├── 07-state-management.md
│   ├── 08-next-steps.md
│   ├── 09-push-notifications.md
│   └── 10-backend-api-guide.md
└── backend-architecture/
    ├── mio-system-architecture.md
    └── mio-ai-architecture.md
```

---

## 문서 읽기 전략

설계 작업을 시작할 때 **모든 문서를 한꺼번에 읽지 말 것.**

1. **항상 `frontend-design/index.md`를 먼저 읽는다** — 전체 구조·기술 스택·핵심 플로우 파악
2. 작업에 필요한 문서만 선택적으로 추가 읽는다

| 작업 유형 | 읽어야 할 문서 |
|---|---|
| 특정 페이지 설계 수정 | `index.md` → 해당 `04-pages-*.md` |
| 공통 컴포넌트 추가/수정 | `index.md` → `05-components.md` |
| 네비게이션 변경 | `index.md` → `03-navigation.md` |
| 상태 관리 수정 | `index.md` → `07-state-management.md` |
| API 연동 관련 작업 | `index.md` → `10-backend-api-guide.md` |
| 폴더 구조 변경 | `index.md` → `06-folder-structure.md` |
| 전체 온보딩/개요 파악 | `index.md` → `01-overview.md` |

---

## `frontend-design/index.md` 업데이트 규칙

다음 상황이 발생하면 **반드시 `index.md`의 문서 목록·기술 스택·핵심 플로우 섹션을 함께 업데이트**해야 합니다.

| 트리거 | 업데이트할 항목 |
|---|---|
| 새 문서 추가 (`*.md` 파일 생성) | `## 문서 목록` 테이블에 행 추가 |
| 기존 문서 삭제 | `## 문서 목록` 테이블에서 해당 행 제거 |
| 핵심 사용자 플로우 변경 | `## 핵심 사용자 플로우 (요약)` 코드블록 수정 |
| 기술 스택 변경 (라이브러리 추가·교체·제거) | `## 기술 스택 한눈에 보기` 테이블 수정 |

`index.md`를 업데이트하지 않은 채로 문서 작업을 완료했다고 보고하지 말 것.

---

## 설계 작업 흐름

```
사용자 요청
    │
    ▼
① index.md 읽기 (전체 맥락 파악)
    │
    ▼
② 필요한 문서만 추가로 읽기
    │
    ▼
③ ★★★ 사용자에게 질문 먼저 ★★★
   (질문 없이 설계 진행 금지)
    │
    ▼
④ 사용자 답변 수령
    │
    ▼
⑤ 설계 문서 작성·수정
    │
    ▼
⑥ index.md 업데이트 필요 여부 확인 후 처리
```

---

## 파일 네이밍 규칙

새 문서를 추가할 때 아래 규칙을 따른다.

- 파일명은 `##-category-detail.md` 형식 (소문자, 하이픈 구분)
- 페이지 설계 문서는 `04-pages-{페이지명}.md`
- 번호는 `index.md`의 문서 목록 순서와 일치해야 함
- 문서 상단에 반드시 작성일, 관련 문서 링크 포함

---

## 금지 사항

- 설계 내용을 AI가 임의로 결정하는 것
- `frontend-design-plan.md` (통합 원본)에 직접 설계 내용 추가하는 것
- `index.md` 업데이트 없이 문서를 추가·삭제하는 것
- 질문 없이 사용자 의도를 추측하여 문서를 완성하는 것
